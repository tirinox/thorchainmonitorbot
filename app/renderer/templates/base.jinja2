<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <style>
        @font-face {
            font-family: ThorFont;
            src: url(renderer/static/ThorFont.ttf);
        }

        @font-face {
            font-family: ThorFontLight;
            src: url(renderer/static/Exo2-VariableFont_wght.ttf);
        }

        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }


        /* Dynamically set the viewport size */
        html, body {
            margin: 0;
            padding: 0;
            font-family: ThorFontLight, Arial, sans-serif;
            font-weight: 100;
            overflow: hidden; /* Prevent scrollbars */
            position: fixed;
            width: {{ width }}px;
            height: {{ height }}px;
            background-color: #061913;
        }


        div {
        {% if debug %}
            border: #083a34 1px dashed;
        {% endif %}
        }

        body {
            font-family: ThorFont, Arial, sans-serif;
            color: white;
            background: #061913;
        {% if debug %}
            border: 1px darkcyan dashed;
        {% endif %}

        }

        h1 {
            color: #FFF;
        }

        p {
            font-size: 14px;
        }

        .highlight-blue {
            color: #00CCFF;
        }

        .yggdrasil-green {
            color: #33FF99;
        }

        .midgard-turqoise {
            color: #23DCC8;
        }

        .night-black {
            background-color: #101921;
        }

        .container {
            position: relative;
            margin: 0;
            padding: 0;
            width: 100vw;
            height: 100vh;
            border: 1px rgba(255, 255, 255, 0.01) dashed;
            display: block;
            max-width: {{ width }}px;
            max-height: {{ height }}px;
        }

        .title {
            letter-spacing: 0.15em;
            font-family: ThorFontLight, a;
            font-size: 2.7em;
            font-weight: normal;
        }

    </style>
    <script src="renderer/static/chart.umd.min.js"></script>
    {% if auto %}
        <script>
            setInterval(function () {
                location.reload();
            }, 3000);
        </script>
    {% endif %}
</head>
<body class="night-black">
<div class="container">

    {% block content %}
    {% endblock %}

</div>
</body>
</html>
